class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
      
        for i in nums:
            if i == val:
                while nums.count(i) != 0:
                    nums.remove(i)
                return len(nums)
            
