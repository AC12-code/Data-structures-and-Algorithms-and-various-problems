class Solution:
    def predictTheWinner(self, nums: List[int]) -> bool:
        @cache
        def dfs(i,j):
            if i==j:return nums[i]
            l=nums[i]-dfs(i+1,j)
            r=nums[j]-dfs(i,j-1)
            return max(l,r)
  
        return dfs(0,len(nums)-1)>=0
